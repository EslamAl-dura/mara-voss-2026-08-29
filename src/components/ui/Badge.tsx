import { type HTMLAttributes } from 'react';
export function Badge({ className = '', ...props }: HTMLAttributes<HTMLSpanElement>) { return <span className={`inline-flex rounded-full border border-ember px-3 py-1 text-[10px] font-bold uppercase tracking-[.2em] text-ember ${className}`} {...props} />; }