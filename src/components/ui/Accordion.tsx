import { useState } from 'react';
export function Accordion({ items }: { items: { q: string; a: string }[] }) { const [open, setOpen] = useState<number | null>(null); return <div className="divide-y divide-ink/15">{items.map((item, index) => <div key={item.q} className="py-5"><button className="flex w-full justify-between text-left font-bold" onClick={() => setOpen(open === index ? null : index)}><span>{item.q}</span><span className="text-ember">{open === index ? '−' : '+'}</span></button>{open === index && <p className="mt-3 max-w-2xl text-sm leading-7 opacity-70">{item.a}</p>}</div>)}</div>; }